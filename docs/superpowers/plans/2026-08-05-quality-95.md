# OMR Maker Quality 95 Implementation Plan

**Goal:** Raise the verified student and teacher journey quality to an average of at least 95/100, with no core journey below 88/100.

**Architecture:** Keep the existing Next.js client/server boundaries. Repair compatibility and derived-display bugs in pure library helpers first, then wire those helpers into the UI. Validate the current branch (not the older production commit) with focused unit tests, rendered Playwright journeys, and the full lint/test/build suite.

**Tech Stack:** Next.js 16, React 19, TypeScript, Vitest, Playwright, Supabase-backed production fixtures.

---

### Task 1: Lock down the four production High defects

**Files:**
- Modify: `src/lib/distributionTargets.test.ts`
- Create: `src/lib/drawingPath.test.ts`
- Inspect/extend: `e2e/student-dashboard-load-state.spec.ts`
- Inspect/extend: `src/lib/attemptScores.test.ts`

1. Add a failing test proving displayed class membership must be derived from roster rows rather than `RosterGroup.count`.
2. Add failing tests for both current JSON strokes and legacy SVG `M/L` fixture strokes.
3. Confirm current-branch score resolution uses `questionResults` consistently on dashboard, history, review, and retake comparisons.
4. Confirm dashboard does not render zero-value success content before remote assignments finish loading and revalidates after navigation/focus.

### Task 2: Repair distribution target display

**Files:**
- Modify: `src/lib/distributionTargets.ts`
- Modify: `src/components/DistributeModal.tsx`

1. Export a roster-derived per-group member counter using the same region-aware identity matching as the total summary.
2. Replace stale `g.count` rendering in each group row.
3. Run `npx vitest run src/lib/distributionTargets.test.ts`.

### Task 3: Add backward-compatible handwriting rendering

**Files:**
- Create: `src/lib/drawingPath.ts`
- Modify: `src/components/PDFViewer.tsx`
- Modify: `src/lib/annotatedPdfExport.ts`

1. Parse validated normalized JSON strokes.
2. Parse legacy fixture SVG `M/L` polylines and normalize A4 PDF-point coordinates.
3. Use the shared parser for on-screen rendering, stroke erasing, and annotated PDF export.
4. Silently skip corrupt payloads instead of logging expected compatibility errors.
5. Run the drawing-path and annotated-export tests.

### Task 4: Remove remaining medium-severity trust breaks

**Files:**
- Modify: `src/app/page.tsx`
- Modify: `src/components/teacher/student-results/HandwritingPanel.tsx`
- Modify as needed: `src/app/globals.css`
- Extend adjacent source-contract and Playwright tests.

1. Preserve workspace query context when returning from a student login form to role selection.
2. Clarify that the handwriting plan label is the plan captured at submission time, distinct from the teacher’s current plan.
3. Recheck invalid teacher credentials and 375px student dashboard header rendering; only patch if current branch still reproduces them.
4. Treat PDF library warnings as technical debt only if they remain non-actionable and do not affect the user journey.

### Task 5: Rendered journey verification

**Files:**
- Extend focused specs under `e2e/` only where a verified regression lacks coverage.
- Update: `.gstack/qa-reports/qa-report-omr-maker-eight-vercel-app-2026-08-05.md` or create a current-branch successor report.

1. Run focused Chromium journeys for student login/guest, dashboard load, exam/review/retake, teacher login/create/distribute/results/handwriting/analytics/CSV.
2. Run mobile and tablet layout checks at 375px, iPad portrait, and landscape.
3. Use separate browser contexts for cross-session access and revalidation checks.
4. Capture evidence for every previously failing High and Medium issue.

### Task 6: Full verification and scoring

1. Run `npm test`.
2. Run `npm run lint`.
3. Run `npm run build`.
4. Run the relevant full Playwright project(s), escalating to all configured projects if focused checks pass.
5. Re-score every student and teacher journey using the same rubric. Report the actual average, the minimum journey score, remaining limitations, and whether the 95/88 target is genuinely met.

